# AMR-Next handoff - 2026-07-20

Snapshot for resuming after a chat-history clear. Supersedes HANDOFF-2026-07-19.md.

## Current state

- Branch `main`, working tree clean, everything pushed. CI green on the tip.
- The "get-to-done" phase is complete: all known bug classes are closed, plus two
  user-reported bugs found and fixed this session (Webtoons reader tab-loop, and
  the Firefox upgrade library-wipe).
- release-please PR **#66** (`chore(main): release 0.13.0`) is open and includes
  every commit below. Hand-written release notes are in `RELEASE_NOTES_v0.13.0.md`.

## Fixes landed this session (all on main, each CI-green, each with tests)

- `6c662a9` v8 cover migration made Firefox-safe (was aborting the upgrade
  transaction and wiping the library on 0.11->0.12); adds a cross-version
  upgrade regression test (`database.upgrade.test.ts`).
- `3d241ab` Webtoons reader Next endless background-tab reopen - a no-op
  chapter-list refresh no longer emits a live event that re-drove the crawl.
- `124c7ab` reading stats aligned to local days (matched to the heatmap);
  download re-resolve no longer mixes pages from two resolutions.
- `8877b0d` UNNUMBERED_SORT_KEY (Infinity) leak class closed: `isNumberedChapter`
  / `latestNumberedChapter` helpers, 5 sites fixed, Dexie v9 repair migration,
  a manga creating/updating tripwire hook, export-path sentinel strip.
- `224e8eb` mgeko search endpoint, mangafreak title/search, madara per-document
  chapter order.
- `72ba621a` dependabot for Actions + repo `CLAUDE.md` guardrails.
- `694efad5` all GitHub Actions off deprecated Node 20.
- `420faea6` prettier CI-break fix + `.gitattributes` (LF) + husky/lint-staged.

## Verified vs still-open

- Self-reviewed (Opus) the Infinity/migration/reducer/webtoons changes: clean.
- The three Fable exit-gate agents from this session STALLED (0-byte output,
  never completed) - not resumed, produced nothing. Their intended checks were
  covered by the self-review and the test suite.
- Two things need a real signed install of 0.13.0 to confirm (a temporary
  about:debugging load ALWAYS starts empty and cannot verify persistence):
    1. Firefox upgrade no longer wipes the library.
    2. Webtoons reader Next no longer loops tabs.

## To cut the release

1. Merge PR #66. release-please tags v0.13.0 and the Release Please workflow
   builds + checksums + uploads the Chrome and Firefox zips to the GitHub
   release. (Owner action - a release is a deliberate step.)
2. Replace the generated release body with `RELEASE_NOTES_v0.13.0.md`.
3. AMO submission is gated behind the `amo` environment - approve it in the
   Actions UI when ready for Mozilla's review queue (see AMO memory + RELEASING.md).
